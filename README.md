# Auto1_Task
Automation script for search functionality

The scenario is automated with selenium webdriver using Java and TestNG framework.
You can find the test case script in SearchFunctionality.java file.
emailable-report.html inside the test-output folder contains the test report
The project folder can directly be imported as a workspace as Eclipse as well.
